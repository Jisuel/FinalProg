@model IEnumerable<FinalProg.Models.Cargos>

@{
    ViewBag.Title = "Lista";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Nuevo Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Cargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cargo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Cargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cargo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.Id_Cargo }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_Cargo }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.Id_Cargo })
        </td>
    </tr>
}

</table>
