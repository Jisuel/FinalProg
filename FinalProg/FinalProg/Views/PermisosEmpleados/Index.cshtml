@model IEnumerable<FinalProg.Models.Permisos>

@{
    ViewBag.Title = "Lista";
}
<link rel="stylesheet" type="text/css" href="~/Content/Style.css" />
<h2>Lista</h2>

<p>
    @Html.ActionLink("Nuevo Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Permisos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Desde)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hasta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comentarios)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empleados.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Permisos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Desde)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hasta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comentarios)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empleados.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.Id_Permisos }, new { @Class = "btn btn-warning"}) |
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_Permisos }, new { @Class = "btn btn-primary" }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.Id_Permisos }, new { @Class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
