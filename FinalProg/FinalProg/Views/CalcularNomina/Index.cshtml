@model IEnumerable<FinalProg.Models.Nomina>

@{
    ViewBag.Title = "Lista";
}
<link rel="stylesheet" type="text/css" href="~/Content/Style.css" />

<h2>Lista</h2>

<p>
    @Html.ActionLink("Nuevo Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Nomina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Nomina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.Id_Nomina }, new { @Class = "btn btn-warning" }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_Nomina }, new { @Class = "btn btn-primary" }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.Id_Nomina }, new { @Class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
